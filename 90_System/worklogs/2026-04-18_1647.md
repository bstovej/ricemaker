# Session Work Log - 2026-04-18

## Summary
Updated the `README.md` file with comprehensive Git and Docker instructions to improve the onboarding and deployment experience for Windows and macOS users.

## Changes
- **README.md**:
    - Added "Cloning the Repository" section with commands for macOS/Linux and Windows.
    - Added detailed "Configuration" section explaining `keys.json` and `config.json`.
    - Added "Docker Deployment" section with commands for `build`, `up`, `logs`, and `down`.
    - Added "Hardware Acceleration" section explaining how to connect to host-side Ollama.
    - Updated Windows path warnings for JSON configuration.
- **.gitignore**:
    - Added `90_System/` to ignored patterns to prevent session logs and system-specific data from being committed.

## Impact
Users can now easily clone and set up the project on their local machines using standard Git and Docker workflows, with clear guidance on hardware acceleration for local LLMs.
